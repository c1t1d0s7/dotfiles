// Custom VS Code keybindings.
[
    // Continue a terminal command on the next line.
    {
        "key": "shift+enter",
        "command": "workbench.action.terminal.sendSequence",
        "args": {
            "text": "\\\r"
        },
        "when": "terminalFocus && !isWindows"
    }
]
